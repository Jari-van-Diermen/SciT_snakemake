title: "sciT report"
subtitle: "Human samples"
intro_text: "This is a quality report generated by the sciT pipeline"
report_comment: "You can add a comment here by editing the config.yaml file"

section_comments:
  QC_transcriptome_clustering: Write a comment here about the clustering

top_modules:
  - "scmo"
  - "samtools"
  - "deepTools"

custom_plot_config:
  scmo_meta:
      defaultsort:
        - column: "RNA_unique"

table_columns_placement:
  scmo_meta:
    Library: 0
    # These are variables used to condition the samples (this is experiment specific)
    number: 1
    #
    DamID_reads: 10
    DamID_unique: 20
    DamID Mapping %: 30
    DamID Usable %: 40
    RNA_reads: 100
    RNA_unique_genes: 105
    RNA_unique: 110
    RNA Mapping %: 120
    RNA Usable %: 130
    Well: 800
